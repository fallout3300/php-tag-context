node_modules/      # Сторонние зависимости (устанавливаются через npm/yarn)
out/               # Результат компиляции TypeScript (JS-файлы)
dist/              # Папка для упакованного расширения (если используете webpack/esbuild)
*.vsix             # Готовые пакеты расширения (бинарные файлы)